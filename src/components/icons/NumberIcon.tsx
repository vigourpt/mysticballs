import React from 'react';
import { LucideProps, Hash } from 'lucide-react';

export const NumberIcon: React.FC<LucideProps> = (props) => {
  return <Hash {...props} />;
};

export default NumberIcon;
