import React from 'react';
import { LucideProps, CircleDot } from 'lucide-react';

export const BallIcon: React.FC<LucideProps> = (props) => {
  return <CircleDot {...props} />;
};

export default BallIcon;
