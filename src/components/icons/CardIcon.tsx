import React from 'react';
import { LucideProps, Scroll } from 'lucide-react';

export const CardIcon: React.FC<LucideProps> = (props) => {
  return <Scroll {...props} />;
};

export default CardIcon;
