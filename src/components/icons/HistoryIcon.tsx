import React from 'react';
import { LucideProps, History } from 'lucide-react';

export const HistoryIcon: React.FC<LucideProps> = (props) => {
  return <History {...props} />;
};

export default HistoryIcon;
