import React from 'react';
import { LucideProps, Sparkles } from 'lucide-react';

export const OracleIcon: React.FC<LucideProps> = (props) => {
  return <Sparkles {...props} />;
};

export default OracleIcon;
