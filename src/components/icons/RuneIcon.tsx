import React from 'react';
import { LucideProps, Asterisk } from 'lucide-react';

export const RuneIcon: React.FC<LucideProps> = (props) => {
  return <Asterisk {...props} />;
};

export default RuneIcon;
