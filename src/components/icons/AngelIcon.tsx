import React from 'react';
import { LucideProps, Feather } from 'lucide-react';

export const AngelIcon: React.FC<LucideProps> = (props) => {
  return <Feather {...props} />;
};

export default AngelIcon;
