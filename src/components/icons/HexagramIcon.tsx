import React from 'react';
import { LucideProps, Hexagon } from 'lucide-react';

export const HexagramIcon: React.FC<LucideProps> = (props) => {
  return <Hexagon {...props} />;
};

export default HexagramIcon;
